<% include partials/pageopen.ejs %>


<!-- <h1>Welcome to EventSet!</h1>
<% if (user) { %>
<h2>You are logged in</h2>
<% } else { %>
<h2>Logged out</h2>
<% } %>
<p><a href="/contact">Contacts</a></p> -->


<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Navigation: </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link first-link" href="/contact">Contacts</a>
        </li>
        <li class="nav-item active">
          <a class="nav-link second-link" href="/event">Events</a>
        </li>
        <li class="nav-item active">
            <a class="nav-link second-link" action="/users/logout">Log out</a>
          </li>
      </ul>
    </div>
  </nav>

<div class="jumbotron gradient" style="height: 100vh; border-radius: 0em;">
    <h1 class="display-4 h1">Welcome to EventSet!</h1>
    <% if (user) { %>
    <p class="lead">You are now logged in.</p>
    <% } else { %>
    <p class="lead">You are logged out.</p><% } %>
    <p>You currently have no events scheduled</p>
    <p><ul><li>This could be the first event but not indented So fun!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!1</li></ul></p>
    <hr class="my-4" id="jumbo-line">
</div>


<% include partials/pageclose.ejs %>